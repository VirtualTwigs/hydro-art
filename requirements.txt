# Runtime dependencies.
# Remaining GIS/rendering dependencies (networkx, svgwrite, svgo, etc.) are
# added by later roadmap items as those stages are implemented.
pyyaml>=6.0
rich>=13.0

# GIS data loading & geometry repair (roadmap item #3)
shapely>=2.1
geopandas>=1.0
pyogrio>=0.10
numpy>=2.0

# River-network graph (roadmap item #5)
networkx>=3.0

# Development / testing
pytest>=8.0

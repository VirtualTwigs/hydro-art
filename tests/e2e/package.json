{
  "name": "hydro-art-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the Hydro-Art alpha customer journey (Epoch 24). Non-offline / opt-in: needs a live serve.py + a pre-extracted county.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
